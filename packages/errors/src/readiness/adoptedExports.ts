/**
 * The package's callable exports. Adoption is counted in calls to them.
 *
 * @internal
 */
export const ADOPTED_EXPORTS: readonly string[] = ['assertIsError', 'chainError', 'describeError', 'isError'];
