// For an explanation of this file's purpose, see the file of the same name in the monorepo root.
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true, // ensure that nobody can accidentally use this config for a build
  },
  "exclude": [],
  "include": [
    "src/", //
    "eslint.config.js",
    "*.js",
    "*.json",
    "*.ts",
  ],
}
