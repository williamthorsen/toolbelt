// Defines aliases that should be used instead of actual workspace name.
export const workspaceToAliasLookup: Record<string, string> = {
  // Some examples from other projects:
  // typescript: 'ts'
  // 'chrome-extension': 'ce'
};
